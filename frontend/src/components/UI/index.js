import Button from "./Button.vue"

export default [
    Button,
]

// здесь импортируем компоненты из UI, чтобы не объявлять их на каждой странице отдельно
// компоненты не из UI будет отдельно объявлять на страницах, тк они используются реже